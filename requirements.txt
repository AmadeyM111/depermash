# ── Core ────────────────────────────────────
numpy>=1.26

# ── Russian NER ─────────────────────────────
natasha>=1.6.0

# ── Multilingual NER (Presidio) ─────────────
presidio-analyzer>=2.2.354
presidio-anonymizer>=2.2.354
spacy>=3.7

# ── Alternative NLP engine (optional) ───────
stanza>=1.8        # опционально; нужна модель: python -c "import stanza; stanza.download('ru')"

# ── spaCy models (install separately) ───────
# python -m spacy download ru_core_news_sm
# python -m spacy download en_core_web_sm

# ── File processing ──────────────────────────
pymupdf>=1.24      # PDF (import fitz)
python-docx>=1.1   # DOCX

# ── Tabular data (K-anonymity / DP) ─────────
pandas>=2.2
openpyxl>=3.1      # XLSX

# ── Image processing (OCR) ───────────────────
Pillow>=10.0       # Работа с изображениями
pytesseract>=0.3   # Tesseract OCR wrapper
# Системная зависимость: tesseract-ocr
# Ubuntu/Debian: sudo apt-get install tesseract-ocr tesseract-ocr-rus
# macOS: brew install tesseract tesseract-lang
# Windows: скачать установщик с https://github.com/UB-Mannheim/tesseract/wiki

# ── CSV encoding detection (optional) ─────────
chardet>=5.0       # Автоопределение кодировки CSV
